/**
 * Модуль контролов
 *
 * Экспортирует типы и интерфейсы для контролов редактора.
 */

// Типы и интерфейсы
export { TransformMode } from './types';

export type {
    IBaseMesh,
    TreeItemData,
    ISelectControl,
    HistoryAction,
    HistoryEntry,
    IHistoryControl,
    ITransformControl,
    ControlButton,
    IControlManager,
} from './types';
