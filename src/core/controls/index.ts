/**
 * Модуль контролов
 *
 * Экспортирует типы и интерфейсы для контролов редактора.
 */

// Типы и интерфейсы
export { TransformMode } from './types';

export type {
    IBaseMesh,
    TreeItemData,
    ITransformControl,
    ControlButton,
    IControlManager,
    ISizeControl,
    ICameraControl,
} from './types';
