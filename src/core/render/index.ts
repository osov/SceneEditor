/**
 * Модуль рендеринга
 *
 * Экспортирует типы и интерфейсы для сервисов рендеринга.
 */

// Типы и интерфейсы
export {
    RenderLayers,
    MaterialUniformType,
    ObjectTypes,
} from './types';

export type {
    RenderSize,
    IRenderEngine,
    TextureData,
    TextureInfo,
    MaterialInfo,
    IResourceManager,
    BaseEntityData,
    IBaseEntity,
    ISceneManager,
    RenderEngineConfig,
    SceneManagerConfig,
    ResourceManagerConfig,
} from './types';
